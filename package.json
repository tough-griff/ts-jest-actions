{
  "name": "ts-jest-actions",
  "private": true,
  "version": "1.0.0",
  "description": "",
  "main": "lib/index.js",
  "scripts": {
    "prebuild": "rimraf lib/",
    "build": "tsc --build src/ --verbose",
    "test": "jest --coverage"
  },
  "author": "Griffin Yourick <gryphon92@gmail.com>",
  "license": "MIT",
  "dependencies": {
    "config": "^3.3.1"
  },
  "devDependencies": {
    "@types/config": "^0.0.36",
    "@types/jest": "^26.0.3",
    "@types/rimraf": "^3.0.0",
    "jest": "^26.1.0",
    "rimraf": "^3.0.2",
    "ts-jest": "^26.1.1",
    "typescript": "^3.9.6"
  },
  "jest": {
    "preset": "ts-jest",
    "testEnvironment": "node"
  }
}
